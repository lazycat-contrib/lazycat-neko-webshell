This directory vendors generated Cap'n Proto Rust bindings and the Cloudflare
tunnel CA certificate used by the Cloudflare Quick Tunnel provider.

The files are derived from `libcfd` 0.1.x so the release build does not require
the external `capnp` compiler to be installed on the target build machine.
